# osc/

## 責務

- `oscBridge.ts`: `grbl/state.ts` の状態変化(`onChange`)を購読し、ワーク座標(WPos)をOSCで送信するのみ。

## やってはいけないこと(不変条件)

- GRBLプロトコルの解析やシリアル通信を行わないこと(`state.ts` から受け取る `AppState` のみを参照する)。
- Renderer描画レートに同期させないこと。状態が変化した時点で即座に送信する(ポーリング間隔とは独立)。
- 値が変化していない場合は再送信しない(直前送信値とのdedupチェックを行う)。
